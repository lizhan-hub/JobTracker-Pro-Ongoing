# ===================================================
# == .env file for LOCAL PRODUCTION-LIKE TESTING ==
# ===================================================

# 数据库配置 (指向我们即将启动的本地DB容器)
# 注意：主机名是服务名 "db"，而不是 "localhost"，用docker启动后localhost指的是backend的容器（一共四个容器backend，frontend，ai_service,db,详见.docker-compose.no-db-local-test.yml文件)
# 这里应该连的是docker-compose.no-db-local-test.yml里的db
SPRING_PROFILES_ACTIVE=prod
DB_URL=jdbc:postgresql://db:5432/jobtracker
DB_USERNAME=admin
DB_PASSWORD=password123

# JWT 密钥 (请替换为一个长且随机的字符串)
JWT_SECRET=your_super_long_and_secret_jwt_key_for_local_test

# 内部服务通信密钥 (请替换为一个长且随机的字符串)
APP_INTERNAL_API_KEY=your_super_long_and_secret_internal_api_key_for_local_test

# AWS 配置 (在本地测试阶段，这些可以是虚拟值，因为我们还不会连接到真实的AWS)
AWS_REGION=us-west-2
AWS_ACCESS_KEY_ID=dummy_access_key
AWS_SECRET_ACCESS_KEY=dummy_secret_key
S3_BUCKET_NAME=dummy-bucket-name
